import{_ as t,c as o,a3 as a,o as i}from"./chunks/framework.CGQ59HuL.js";const b=JSON.parse('{"title":"DA","description":"","frontmatter":{"head":[["meta",{"name":"og:title","content":"DA | Rollkit"},{"name":"og:description","content":false}]]},"headers":[],"relativePath":"learn/specs/da.md","filePath":"learn/specs/da.md","lastUpdated":1752767714000}'),r={name:"learn/specs/da.md"};function l(s,e,n,c,d,h){return i(),o("div",null,e[0]||(e[0]=[a('<h1 id="da" tabindex="-1">DA <a class="header-anchor" href="#da" aria-label="Permalink to &quot;DA&quot;">​</a></h1><p>Rollkit provides a generic <a href="https://github.com/rollkit/rollkit/blob/main/core/da/da.go#L11" target="_blank" rel="noreferrer">data availability interface</a> for modular blockchains. Any DA that implements this interface can be used with Rollkit.</p><h2 id="details" tabindex="-1">Details <a class="header-anchor" href="#details" aria-label="Permalink to &quot;Details&quot;">​</a></h2><p><code>Client</code> can connect via JSON-RPC transports using Rollkit&#39;s <a href="https://github.com/rollkit/rollkit/tree/main/da/jsonrpc" target="_blank" rel="noreferrer">jsonrpc</a> implementations. The connection can be configured using the following cli flags:</p><ul><li><code>--rollkit.da.address</code>: url address of the DA service (default: &quot;grpc://localhost:26650&quot;)</li><li><code>--rollkit.da.auth_token</code>: authentication token of the DA service</li><li><code>--rollkit.da.namespace</code>: namespace to use when submitting blobs to the DA service</li></ul><p>Given a set of blocks to be submitted to DA by the block manager, the <code>SubmitBlocks</code> first encodes the blocks using protobuf (the encoded data are called blobs) and invokes the <code>Submit</code> method on the underlying DA implementation. On successful submission (<code>StatusSuccess</code>), the DA block height which included in the blocks is returned.</p><p>To make sure that the serialised blocks don&#39;t exceed the underlying DA&#39;s blob limits, it fetches the blob size limit by calling <code>Config</code> which returns the limit as <code>uint64</code> bytes, then includes serialised blocks until the limit is reached. If the limit is reached, it submits the partial set and returns the count of successfully submitted blocks as <code>SubmittedCount</code>. The caller should retry with the remaining blocks until all the blocks are submitted. If the first block itself is over the limit, it throws an error.</p><p>The <code>Submit</code> call may result in an error (<code>StatusError</code>) based on the underlying DA implementations on following scenarios:</p><ul><li>the total blobs size exceeds the underlying DA&#39;s limits (includes empty blobs)</li><li>the implementation specific failures, e.g., for <a href="https://github.com/rollkit/rollkit/tree/main/da/jsonrpc" target="_blank" rel="noreferrer">celestia-da-json-rpc</a>, invalid namespace, unable to create the commitment or proof, setting low gas price, etc, could return error.</li></ul><p>The <code>RetrieveBlocks</code> retrieves the blocks for a given DA height using <code>GetIDs</code> and <code>Get</code> methods. If there are no blocks available for a given DA height, <code>StatusNotFound</code> is returned (which is not an error case). The retrieved blobs are converted back to blocks and returned on successful retrieval.</p><p>Both <code>SubmitBlocks</code> and <code>RetrieveBlocks</code> may be unsuccessful if the DA node and the DA blockchain that the DA implementation is using have failures. For example, failures such as, DA mempool is full, DA submit transaction is nonce clashing with other transaction from the DA submitter account, DA node is not synced, etc.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><p>[1] <a href="https://github.com/rollkit/rollkit/blob/main/core/da/da.go#L11" target="_blank" rel="noreferrer">da-interface</a></p><p>[2] <a href="https://github.com/rollkit/rollkit/tree/main/da/jsonrpc" target="_blank" rel="noreferrer">jsonrpc</a></p>',14)]))}const m=t(r,[["render",l]]);export{b as __pageData,m as default};
